# Session 2026-05-27 04:00 — 终端三修（滚动/调整/光标）

## 完成内容

1. **`margins=None` AttributeError 修复**
   - `_ScrollbackScreen.index()` 在 pyte 未设置滚动区时 `self.margins` 为 `None`，访问 `.bottom` 报错
   - 修复：`if self.margins is None` 分支使用全屏默认值（`cursor.y == lines - 1`）

2. **鼠标滚轮滚动历史**
   - 2000 行 scrollback deque，`on_mouse_scroll_up/down` 调整 `_scroll_offset`
   - `border_subtitle` 显示"↑N行"偏移量提示
   - 新输出到达时自动跳回底部

3. **拖拽调整终端高度**
   - 新增 `_ResizeHandle` widget，置于 `TerminalStrip` 顶部
   - `capture_mouse()` + `on_mouse_move` 计算 delta，`strip.styles.height` 动态赋值
   - 范围限制：5-40 行

4. **光标不溢出边框**
   - `on_resize` 改用 `self.content_size`（扣除 border 后的内容区）同步给 pyte screen
   - 之前用 `event.size`（外部尺寸）导致 pyte 屏幕比内容区宽，光标渲染越界

## 测试状态

91 测试全部通过（含 `test_pty_terminal_receives_output` 之前一直失败的用例）

## 下次从哪里开始

feat-008：智能体终端浮窗（复用 PtyTerminal，包装进 FloatWindow）
- 依赖 feat-003（已完成）
- 核心工作：`AgentTerminalWindow(FloatWindow)` + `PtyTerminal` 组合
- 焦点隔离：浮窗内终端与底部 bash 终端互不干扰
