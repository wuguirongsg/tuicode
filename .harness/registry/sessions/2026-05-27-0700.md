# Session 2026-05-27 07:00 — 赛博朋克 UI (feat/cyberpunk-ui)

## 阶段
BUILD — 新分支 `feat/cyberpunk-ui`

## 完成

### 赛博朋克视觉重设计（单 commit）

**`float_window.py`** — 彻底移除 TitleBar / WinButton 独立组件：
- 改用 Textual `border-title` 把 `[●] [●] [●]  Title` 直接嵌入边框线（节省 1 行高度）
- 每个浮窗循环取霓虹色（青 #00d4ff / 紫 #bf00ff / 粉 #ff00a0 / 绿 #00ff41 / 金 #ffb800）
- 顶部边框行 (screen_y == region.y) 检测鼠标：col 2/4/6 = 关闭/最小化/最大化，其余区域拖动
- 焦点/子焦点时边框高亮为近白色 #e0f7ff

**`workspace.py`** — 空态背景升级：
- `_CyberpunkHint`：大号 TUICODE ASCII 艺术字（3 层渐变蓝）+ 双帧闪烁机器人（0.9 秒间隔）+ 副标题装饰线
- `open_window()` 淡入动画：opacity 0 → 1，0.25 秒

**`app.py`** — 注册 cyberpunk 主题：
- background `#000510`（近黑），surface `#060d1f`，panel `#0a1428`
- primary 霓虹青，secondary 霓虹紫，accent 热粉

**`editor_window.py`** — 适配新架构：
- 移除 TitleBar 依赖；覆盖 `_do_close()` 保留未保存确认逻辑
- `_refresh_title()` 改为更新 `border_title`（调 `_refresh_border()`）

**测试** — 89 个全绿：
- `test_float_window.py`：移除 TestTitleBar，所有按钮测试改用 `_do_close()` / `_do_min_toggle()` / `_do_max_toggle()`
- `test_taskbar.py`：`pilot.click("#btn-*")` 全部改为方法调用；TestDragBoundary 移除 TitleBar 引用，改为直接验证夹值逻辑

## 未完成
feat-008 / feat-010 / feat-011 / feat-012（智能体相关功能）未动

## 下次从哪开始
当前分支 `feat/cyberpunk-ui`，可视觉确认后合并到 main，然后继续 feat-008（智能体终端浮窗）。
