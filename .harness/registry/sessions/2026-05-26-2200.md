# Session 2026-05-26 22:00 — feat-003 收尾 + feat-004 完成

## 完成了什么

**feat-003 收尾修复（3 项）**
- 拖拽边界：`on_title_bar_drag_moved` 用 `region.y` 推导 `natural_y`，将 actual_y 夹住在 `[0, ws_h-1]`，x 夹住在 `[0, ws_w-20]`
- 标题栏点击置顶：`TitleBar.on_mouse_down` 在 `event.stop()` 前调用 `parent._bring_to_top()`
- ResizeHandle 扩宽至 3 格，render `" ◢ "`

**feat-004 窗口任务栏（全部实现）**
- `TaskButton`：显示窗口标题 + 最小化图标（▾/▪），点击置顶/还原
- `WindowTaskBar`：列出所有窗口，管理按钮增删，`get_window_at(n)` 支持 Alt+N
- `FloatWorkspace.WindowOpened` 消息 → App 同步任务栏
- `FloatWindow.MinimizeToggled` 消息 + `restore()` 公共方法
- `AgentDeckApp`：`Alt+1/2/3` 快切，`on_float_workspace_window_opened` / `on_float_window_closed` / `on_float_window_minimize_toggled` 处理器
- 28 个单元测试全部通过（feat-003 × 16 + feat-004 × 12）

## 未完成

- 视觉确认（用户尚未运行 `Ctrl+T` 验证）
- `feat/003-float-window` 分支尚未 merge 到 master

## 下次从哪里开始

1. 用户视觉确认 feat-003/004 效果
2. `git checkout master && git merge feat/003-float-window`
3. 继续 feat-005（文件树）或 feat-006（编辑器浮窗）
