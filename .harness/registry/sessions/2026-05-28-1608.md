# Session Summary — 2026-05-28 16:08

## 阶段

BUILD

## 完成

- 完成 feat-014 Git diff 只读预览：
  - 新增 `GitDiffService`，按文件读取 working tree / staged / untracked diff，不修改工作区或暂存区。
  - 新增 `DiffPreviewWindow`，用只读 `TextArea` 展示 diff。
  - 右侧 Git 面板新增可聚焦 `GitFileList`，支持键盘上下选择、回车打开 diff，以及点击打开 diff。
  - `TuiCodeApp` 响应 `RightPanel.DiffRequested`，在浮窗工作区打开 diff 预览。
- `features.json` 中 `feat-014` 已标记为 `passes=true`。

## 验证

- `PYTHONPATH=src python -m pytest -q tests/test_git_diff.py tests/test_diff_preview_window.py tests/test_file_tree.py tests/test_app_integration.py`：15 passed。
- `PYTHONPATH=src python -m pytest -q`：117 passed。

## 未完成 / 下次建议

- 继续 Phase 2A 的 `feat-015`：Git stage / unstage / commit 最小闭环。
- 当前仍有用户未提交改动：`.codex/config.toml` 与 `skills/` 下两个 skill 文件，本次未触碰。
