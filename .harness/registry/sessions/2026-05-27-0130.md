# Session 2026-05-27 01:30

## 阶段
BUILD

## 完成了什么

### 分支合并
- `feat/005-006-file-tree-editor` 合并到 `master`（no-ff merge）
- 创建 `feat/007-bash-terminal` 开发分支

### feat-007：底部 bash 终端
- 新建 `src/agentdeck/ui/pty_terminal.py`：
  - `PtyTerminal` Widget，PTY + pyte 虚拟终端
  - `os.openpty()` + `asyncio.create_subprocess_exec` 启动 bash
  - `loop.add_reader()` 非阻塞监听 master fd（关键：避免 `run_in_executor + os.read` 阻塞线程导致测试卡死）
  - `pyte.Screen` 解析 ANSI，`render_line()` 逐行转 Rich Segment（支持 vim/top 等全屏 TUI）
  - `TIOCSWINSZ` 同步终端尺寸，完整按键映射（方向键/Ctrl+C/F1-F10）
  - `_on_unmount` 清理 fd reader、terminate 子进程
- 更新 `src/agentdeck/ui/terminal_strip.py`：替换 placeholder，嵌入 `PtyTerminal`
- `pyproject.toml` 新增 `pyte>=0.8.0` 依赖
- `tests/test_pty_terminal.py`：11 个测试（颜色转换、mounts、输出接收、Ctrl+C、resize、render）

## 测试结果
- feat-007 专项：11 通过
- 全量：91 通过，0 失败，无回归

## 未完成

- feat-007 视觉验收未做（需人工启动 `python -m agentdeck` 手动测试 `ls --color` / `vim` / `top`）
- feat-008 ~ feat-012 均未开始

## 下次从哪里开始

1. 人工视觉验收 feat-007（建议先跑 app 确认 bash 终端渲染正常）
2. 开始 feat-008（智能体终端浮窗），依赖 feat-007 的 PtyTerminal，可复用
