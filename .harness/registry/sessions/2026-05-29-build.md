# Session 2026-05-29 — Phase 2A BUILD（feat-015 ~ feat-018）

## 完成了什么

### feat-015 Git stage/unstage/commit 最小闭环
- `git_status.py` 新增 `GitOps` 类（`stage/unstage/commit`），`unstage` 在无 HEAD 时自动回退 `git rm --cached`
- `right_panel.py` 新增 `CommitBar`（Input + 状态行），`GitFileList` 支持 `s`/`u` 键触发 `StageRequested`
- `RightPanel` 处理 stage 操作和 commit 提交，成功清空输入，失败保留并显示错误

### feat-016 布局预设 Ctrl+1/2/3
- `workspace.py` 新增 `_preset_positions()` 计算三种预设：编辑（主窗口全屏）、双 Agent（左右平分）、调试（上 70% 下 30%）
- `FloatWorkspace.apply_preset(n)` 遵循 `_stack_y` 补偿机制重排窗口
- `app.py` 注册 `Ctrl+1/2/3 → action_layout_preset`

### feat-017 多 Agent PTY 会话启动器
- `AgentTerminalWindow` 新增 `session_id`（UUID4 前 8 位）、`agent_type`、`is_running` 属性
- `new_agent_modal.py` 新建 `NewAgentModal`：预设（Claude Code / Aider / Bash）+ 自定义命令输入
- `Ctrl+T` 改为 push NewAgentModal，dismiss 后打开对应 AgentTerminalWindow

### feat-018 命令面板 Ctrl+Shift+P
- `command_palette_modal.py` 新建 `CommandPaletteModal`：搜索框 + `_CommandList.render()` 整体渲染（避免动态 mount 竞态）
- BINDINGS 处理 escape/down/up，`on_input_submitted` 处理 enter 执行
- 10 条命令收录：Agent 启动、布局预设、终端焦点、窗口切换、Git commit、退出

## 验证

- `PYTHONPATH=src pytest tests/ -q`：172 passed（本次新增 31 条测试）
- features.json：feat-015/016/017/018 全部 passes=true

## Phase 2A 完成标准达成

features.json 中 feat-013 至 feat-018 全部 `passes = true` ✓
